# ==========================================
# 账户私钥配置 (⚠️ 仅用于测试账号!)
# ==========================================

# 主账户（Owner/部署者）- 必填
PRIVATE_KEY=your_private_key_here

# 测试账户（可选，用于多账户交互测试）
# 从 MetaMask 导出多个测试账户的私钥
PRIVATE_KEY_USER1=your_second_account_private_key
PRIVATE_KEY_USER2=your_third_account_private_key

# 提示：
# 1. 从 MetaMask 导出私钥：账户详情 → 导出私钥
# 2. 建议创建新的测试专用账户，不要使用有真实资产的账户
# 3. 每个测试账户需要一些测试 ETH 用于支付 gas

# ==========================================
# RPC 节点 URL (用于连接区块链)
# ==========================================
# Ethereum Sepolia - 推荐使用 Alchemy
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Base Sepolia - 可用免费官方节点或 Alchemy
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
# 或使用 Alchemy: https://base-sepolia.g.alchemy.com/v2/YOUR_BASE_KEY

# ==========================================
# Etherscan API Key (用于合约验证)
# ==========================================
# ⭐ 只需要一个 Etherscan API Key！
# 从 https://etherscan.io 申请，可用于验证所有链的合约
# (包括 Ethereum、Base、Optimism、Arbitrum 等)
ETHERSCAN_API_KEY=your_etherscan_api_key

# ⚠️ 注意：Basescan 已合并到 Etherscan API V2
# 不再需要单独的 BASESCAN_API_KEY

